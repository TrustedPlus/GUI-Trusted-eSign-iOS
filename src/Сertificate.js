import React, { Component } from 'react';
import { Button } from 'react-native';

export default class Сertificate extends Component {
   
  static navigationOptions = ({navigation}) => ({
    title: 'Управление сертификатами',
  });
  render() {
    return (
      null
    );
  }
}