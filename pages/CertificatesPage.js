import React, { Component } from 'react';
import { Platform, ActivityIndicator, StyleSheet, Image, Text,TextInput, TouchableHighlight, View, NavigatorIOS} from 'react-native';

export default class CertificatesPage extends Component {
   
    render() {
      return (
        <Text>O, hey Mark</Text>
      );
    }
}